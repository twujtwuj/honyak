[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "honyak"
version = "0.1.0"
description = "English → Toki Pona translation project"
dependencies = [
    "torch",
    "transformers",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["honyak"]